/**
 * AI Utility Module — Sandbox Layer
 * Provides helper functions for formatting, conversions, etc.
 */

export function formatInsight(text) {
  return {
    formatted: true,
    message: text.trim(),
    length: text.length
  };
}
