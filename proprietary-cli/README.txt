Proprietary CLI (private)

This directory contains a private/closed-source CLI tool. Do NOT commit secrets or keys here.

Files:
 - bin/cli.js  — entrypoint
 - config.example.json — sample config (do not rename to config.json without replacing placeholders)

Keep this folder clean of sensitive data. Use the .gitignore provided to avoid accidental commits.