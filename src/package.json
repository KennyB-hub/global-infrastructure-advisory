{
  "name": "global-infrastructure-advisory",
  "version": "1.2.0",
  "description": "GIA Deep Mind Sovereign Infrastructure Tool",
  "main": "src/index.js",
  "type": "module",

  "scripts": {
    "clean": "node organize.js",
    "scan:deep": "node seven-runtime/tools/run-repo-audit.js",
    "scan:secrets": "node scripts/scan-secrets.cjs",
    "scan:licenses": "node scripts/license-scan.cjs",
    "test:cli-sandbox": "node scripts/test-cli-sandbox.cjs",
    "verify:ws": "node scripts/verify-ws-test.cjs",

    "lint": "eslint . --ext .js,.ts",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "test": "mocha --timeout 10000",

    "audit": "npm audit --audit-level=high",
    "security:all": "npm run scan:secrets && npm run scan:licenses && npm run audit",

    "smoke": "node scripts/smoke-test.cjs",
    "doctor": "npm run clean && npm run scan:deep && npm run security:all && npm run test:cli-sandbox && npm run verify:ws",

    "dev": "npm run clean && wrangler dev",
    "deploy": "npm run clean && wrangler deploy",
    "logs": "wrangler tail",

    "build": "echo 'Build optimized for GIA Sovereign Engine'",
    "precommit": "npm run lint && npm run typecheck && npm run security:all"
  },

  "dependencies": {
    "three": "^0.150.1",
    "@azure/identity": "^3.0.0",
    "@azure/keyvault-secrets": "^4.7.0"
  },

  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
